const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
  theme: {
         extend: {
           fontFamily: {
             syne: ['Syne', 'sans-serif'],
             dm:   ['DM Sans', 'sans-serif'],
           },
         },}
};

export default config;
